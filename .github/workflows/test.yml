name: Test Project
on: push
jobs:
  test:
    runs-on: unbuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v7
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
